[project]
name = "wartungsmanagement-db"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "psycopg[binary]",
    "psycopg-pool",
    "pydantic",
    "uvicorn[standard]",
]