[project]
name = "wartungsmanagement-FE"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "requests",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.3",
]
